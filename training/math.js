// const num1 = 100;
// const num2 = 50;
// let xmx;

// //SIMPLE MATH WITH NUMBERS :
// xmx = num1 + num2;
// xmx = num1 - num2;
// xmx = num1 / num2;
// xmx = num1 * num2;
// xmx = num1 % num2;
// xmx = num1 ** 2;

// // MATH OBJECTS :
// xmx = Math.PI; // get pi number
// xmx = Math.round(2.8);
// xmx = Math.round(2.3); //round to the closest
// xmx = Math.ceil(2.4); // round to top
// xmx = Math.floor(2.9); // round to the bottom
// xmx = Math.abs(-33); // positive version
// xmx = Math.pow(8, 3); // racine (numero , racine )
// xmx = Math.min(10, 15, 23, 7, 88); // return min of series of numbers
// xmx = Math.max(10, 15, 23, 7, 88); // return max of series of numbers
// xmx = Math.random(); // gives a random number from 0 to 0.99
// xmx = Math.round(Math.random() * 20 + 1); // random numb 1-20 wraped in round or floor to get a decimal number

// console.log(xmx);
