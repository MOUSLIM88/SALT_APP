// const numbers = [12, 11, 45, 53, 56, 77, 86, 343, 234, 7667, 879, 97, 9];
// const numbers2 = new Array(34, 66, 75, 78, 11, 7);

// const fruits = ["Bananas", "Pears", "Oranges", "Appels"];
// const mixed = [
//   28,
//   "kochana",
//   true,
//   undefined,
//   null,
//   { a: 3, b: 7, c: 23 },
//   new Date(),
// ];

// let val;

// val = numbers.length; //get length
// val = numbers.push(444); // add a number at the end
// val = Array.isArray(mixed); // to check if something is an array
// numbers[3] = 100; // relpace or edit item at spesific position
// numbers[3] = true;
// numbers2.push("hello");
// val = numbers2.indexOf("hello");
// val = numbers.unshift(555); // add at the front
// val = numbers.pop(); // take of the last item
// val = numbers.shift(); // take of from the front
// val = numbers.splice(1, 2); // take of iteams from specific position
// val = numbers.reverse();
// val = numbers.reverse();

// // Concat arrays
// val = numbers.concat(numbers2);

// // sorting arrays
// val = fruits.sort();
// val = numbers.sort(); // not working with numbers ..look at the method below

// // compare function:
// val = numbers.sort(function (x, y) {
//   return x - y;
// });

// // reverse sorting same function with y-x instead :
// val = numbers.sort(function (x, y) {
//   return y - x;
// });

// // find
// function under50(num) {
//   return num < 50;
// }
// val = numbers.find(under50);

// console.log(numbers);
// console.log(val);
